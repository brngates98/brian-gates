# Optional: use Web3Forms (https://web3forms.com/) instead of FormSubmit for the contact form.
# Leave unset to use FormSubmit (no signup; first submission may require email verification).
# For GitHub Actions: add repository secret WEB3FORMS_ACCESS_KEY with the same value.
PUBLIC_WEB3FORMS_ACCESS_KEY=
